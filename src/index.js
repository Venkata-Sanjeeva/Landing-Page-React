import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../src/components/App.js';  // Importing the App component
//import './assets/index.css'; // Inside your component file

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);  // Rendering the App component


// <App /> is short for <App></App>
